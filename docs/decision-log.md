# Decision Log

> **Synthetic data.** Veridian LegalTech is fictional. See the root [README.md](../README.md) for the full disclaimer.

Every tooling correction, methodology limitation, bug, and deliberate design decision made across this project's build — consolidated here with a one-line summary and a link to where it's actually documented in full. This page is an index, not a re-explanation; read the linked section for the full reasoning, evidence, and verification behind each one.

| # | Stage | Decision or correction | Source |
|---|---|---|---|
| 1 | 3 (research) | Chose CISO Assistant over Eramba — Eramba's Community-edition API access is reported unreliable in practice; CISO Assistant's is confirmed unrestricted | `CLAUDE.md` §2, "Why not Eramba" |
| 2 | 3 (research) | Initial research found no prebuilt n8n↔CISO Assistant connector; automation designed around the standard HTTP Request node + PAT instead | `CLAUDE.md` §2, "Correction from earlier research" |
| 3 | 5 | That initial finding was itself superseded: `n8n-nodes-ciso-assistant` is a real, MIT-licensed, actively maintained community node — confirmed by downloading and inspecting the actual npm tarball, not just the registry page | `CLAUDE.md` §2, "Update (Stage 5)" |
| 4 | 5 | Distinguished the real node from a different, unpublished, AGPL-licensed node bundled inside `ciso-assistant-community` itself, which is missing `requirementAssessment`/`user` support | `CLAUDE.md` §2, same section |
| 5 | 5 | Supply-chain risk disclosure: n8n community nodes run unsandboxed with full credential-store access; using this one is a provenance judgment (published under the platform's own npm org), not a claim that community nodes are safe generally | `CLAUDE.md` §2, "Supply-chain note" |
| 6 | 3 | No hosts-file entry was actually needed for Caddy's TLS/SNI requirement — `localhost` alone satisfies it; CLAUDE.md's anticipated fix only applies if the URL were a bare IP | `docs/stage3-deployment-notes.md`, "Hostname / TLS" |
| 7 | 3 | Asset `type` (`PR`/`SP`) typed by business-criticality (customer-facing/production vs. internal tooling), not strict EBIOS RM semantics, since the latter would have made the field meaningless for this inventory (7 Primary / 8 Support) | `docs/stage3-deployment-notes.md`, "Asset Import — PR/SP Typing Criterion" |
| 8 | 3 | `current_proba`/`current_impact` set identical to inherent for all 16 scenarios rather than inventing a split rule the methodology doesn't define; `risk-register.csv`'s residual columns remain the authoritative record | `docs/stage3-deployment-notes.md`, "Risk Import — Limitation" |
| 9 | 3/4 | Data-integrity bug found in `data/generate_inventory.py`: one employee (EMP-0190) got a `term_date` after the dataset's `AS_OF` reference date. Generator fixed; the already-committed CSV deliberately **not** regenerated, to avoid silently shifting other already-cited counts | `docs/stage3-deployment-notes.md`, "Data Integrity Finding" |
| 10 | 5 | Residual risk mapped into CISO Assistant's independent likelihood/impact indices by reducing **only** likelihood, not both — reducing both would compound to roughly the square of the intended effectiveness percentage | `docs/risk-methodology.md` §4a |
| 11 | 5 | Bug found and fixed: `Finding.severity` is an integer server-side; the read API hydrates it to a label, but `PATCH` requires the raw integer. The escalation logic originally sent the label back and got rejected | `docs/stage5-automation-notes.md`, "Bug Found and Fixed" |
| 12 | 5 | CISO Assistant and n8n run as two separate Docker Compose projects, joined by attaching n8n onto `ciso-assistant-community_default` as an external network, rather than one merged stack | `docs/stage5-automation-notes.md`, "Reaching CISO Assistant from n8n's container"; diagrammed in `docs/architecture.md` |
| 13 | 5 | Caught before use: `risk_scoring.py`'s `--base-url` default was mistakenly copied from the n8n container-network fix (`http://backend:8000`), which only resolves inside Docker — corrected back to `https://localhost:8443` for host-side use | `docs/stage5-automation-notes.md`, final paragraph |
| 14 | 5 | The n8n "owner account" screen reappearing on login looked like a lost-data bug; confirmed via `docker volume inspect` and `n8n list:workflow` that the volume and imported workflow were intact the whole time — a genuine one-time manual step, not data loss | `docs/stage5-automation-notes.md`, "Owner account is a one-time manual step" |
| 15 | 5 | Nodes loaded via `N8N_CUSTOM_EXTENSIONS` get re-namespaced under a `CUSTOM.` prefix at runtime, so workflow JSON shows `CUSTOM.cisoAssistant`, not the package's own declared node type | `docs/stage5-automation-notes.md`, "Community node registration" |
| 16 | 6 | `streamlit-facade` verified real but self-described as "shadcn-inspired" — the exact look it was chosen to avoid — so it was dropped in favor of native theming + hand-rolled CSS | `CLAUDE.md` §3, "Correction (Stage 6)" |
| 17 | 6 | Deliberate, later override of #16: the user explicitly chose shadcn-adjacent polish + Apple-style typography anyway, knowingly accepting some `aegis-triage` visual-convergence risk — recorded as a considered tradeoff, not a silent contradiction | `CLAUDE.md` §3, "Deliberate override (Stage 6, second correction)" |
| 18 | 6 | Heat map originally sourced residual band from the CSV on the mistaken belief that CISO Assistant had no independent residual likelihood/impact data — it does (Stage 5 populates it); corrected to pull live, with per-row CSV fallback, surfacing that 6 of 16 risks' live and CSV bands legitimately disagree | `docs/stage6-dashboard-notes.md`, "Correction: heat map originally sourced..." |
| 19 | 6 | WCAG contrast bug found and fixed: white text on brass/amber fills and amber/brass stamp ink both measured below 4.5:1; added darkened text-safe variants for anything rendered as text rather than a fill | `docs/stage6-dashboard-notes.md`, "Accessibility" |
| 20 | 6 | Bug found and fixed: `st.html()` sanitizes with DOMPurify and silently strips `<link>` tags, so the Phosphor icon font never loaded via that method — fixed via `@import` inside an actual `<style>` block instead | `docs/stage6-dashboard-notes.md`, item 1 of the follow-up fix pass |
| 21 | 6 | A visual artifact next to section headers was root-caused as Streamlit's own native "copy link to heading" icon, not this project's icon code — suppressed since it matched nothing else in the design system | `docs/stage6-dashboard-notes.md`, "shadcn-adjacent polish" section |
| 22 | 6 | Severity indicators in repeated-list contexts (e.g. the Findings list) were four rotated stamps read as visual noise; replaced with a quiet dot + flat label there, reserving the full rotated stamp for single-item detail views only | `docs/stage6-dashboard-notes.md`, same section |
| 23 | 7 (cleanup) | `.cursor/rules/*.mdc` had been tracked since the repo's first commit — the same "local tooling config" category as `.claude/`/`.agents/`, tracked before that reasoning existed. Untracked for consistency, as its own commit | commit `90cff62` |
| 24 | 7 | CLAUDE.md §3 stated the platform stack as "Django/SvelteKit/PostgreSQL/Redis" — the real `docker-compose.yml` defines no Postgres or Redis service at all; it runs on the backend image's embedded SQLite and a file-backed `huey` queue | `docs/architecture.md`, "Correction found while writing this diagram" |
| 25 | 7 | `controls/README.md` and `risk-register/README.md` still described their CSVs as pending the Stage 2 content-ownership rewrite gate; both CSVs have had 0 `DRAFT:` prefixes since Stage 2 completed and the rewrite checklist is 100% checked off. Both status stubs corrected | This commit |
| 26 | 7 | README's real-numbers table stated the residual risk-band distribution as "2 High / 11 Medium / 3 Low" — the CSV's own combined-score band, computed correctly but presented unlabeled as if it were *the* residual figure, colliding with #18's already-documented CSV-vs-live divergence. **Caught by an independent user recheck against a fresh `GET`, not a self-catch**; re-verified live before fixing: 2 High / 9 Medium / 5 Low. Table now labels the figure as live and points to #18 instead of stating one number unqualified | This commit |
